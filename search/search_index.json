{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Kavesh Naidoo","text":"<p>Notes from the intersection of identity, infrastructure, and AI \u2014 Entra ID, PowerShell, and the systems in between. I lead IT/infra and I'm learning to build, so expect equal parts architecture and hands-on.</p> <p>Read the blog \u2192</p>"},{"location":"#what-youll-find-here","title":"What you'll find here","text":"<ul> <li> <p> Identity &amp; Entra ID</p> <p>Practical Microsoft Entra ID and Microsoft Graph \u2014 access packages, group management, and the PowerShell to automate them.</p> </li> <li> <p> PowerShell</p> <p>Copy-pasteable Microsoft Graph PowerShell snippets for day-to-day identity administration.</p> </li> <li> <p> Infrastructure</p> <p>Getting from zero to a working, authenticated app on your own machine.</p> </li> <li> <p> AI</p> <p>Where AI agents are reshaping software \u2014 and the businesses built on it.</p> </li> </ul>"},{"location":"blog/","title":"Blog","text":"<p>Identity, infrastructure, and AI \u2014 with a bias toward things you can run today.</p>"},{"location":"blog/assign-entra-access-packages-powershell/","title":"Assigning Entra ID access packages with PowerShell","text":"<p>Entitlement Management is how Entra ID does access-at-scale: bundle the groups, apps, and roles a role needs into an access package, then assign people to it through a policy. The portal is fine for one person. For onboarding a cohort, or wiring it into a joiner workflow, you want PowerShell.</p> <p>This post walks the full admin-driven assignment: find the package, find the policy, submit the request, confirm it landed.</p> <p>Permissions</p> <p>You need <code>EntitlementManagement.ReadWrite.All</code> (delegated, as an Identity Governance admin) or the equivalent application permission for unattended runs. Access packages are a Microsoft Entra ID Governance (premium) feature.</p> <pre><code>Connect-MgGraph -Scopes \"EntitlementManagement.ReadWrite.All\"\n</code></pre>"},{"location":"blog/assign-entra-access-packages-powershell/#1-find-the-access-package","title":"1. Find the access package","text":"<pre><code>$pkg = Get-MgEntitlementManagementAccessPackage `\n  -Filter \"displayName eq 'Engineering \u2014 Standard Access'\"\n\n$pkg.Id\n</code></pre>"},{"location":"blog/assign-entra-access-packages-powershell/#2-find-the-assignment-policy","title":"2. Find the assignment policy","text":"<p>A package can have several policies (different approval rules, different eligible populations). An admin-add must target a specific one:</p> <pre><code>$policy = Get-MgEntitlementManagementAssignmentPolicy `\n  -Filter \"accessPackageId eq '$($pkg.Id)'\" |\n  Select-Object -First 1\n\n$policy.Id\n</code></pre>"},{"location":"blog/assign-entra-access-packages-powershell/#3-resolve-the-target-user","title":"3. Resolve the target user","text":"<pre><code>$user = Get-MgUser -UserId \"ada.lovelace@contoso.com\"\n$user.Id\n</code></pre>"},{"location":"blog/assign-entra-access-packages-powershell/#4-submit-the-assignment-request","title":"4. Submit the assignment request","text":"<p>The request body is the fiddly part. <code>requestType = \"adminAdd\"</code> is the admin granting access directly (no end-user request, no approval round-trip):</p> <pre><code>$params = @{\n    requestType = \"adminAdd\"\n    assignment = @{\n        targetId           = $user.Id\n        assignmentPolicyId = $policy.Id\n        accessPackageId    = $pkg.Id\n    }\n}\n\n$request = New-MgEntitlementManagementAssignmentRequest -BodyParameter $params\n$request.Id\n$request.State   # e.g. \"submitted\" / \"delivering\"\n</code></pre> <p>Cmdlet and property names track the Graph version</p> <p>The SDK is generated from the Graph metadata, so names shift between v1.0 and beta and across SDK releases. If <code>New-MgEntitlementManagement...</code> errors, confirm the current shape rather than trusting this verbatim:</p> <pre><code>Find-MgGraphCommand -Command \"New-MgEntitlementManagementAssignmentRequest\" |\n  Select-Object Command, Method, Uri, ApiVersion\nGet-Help New-MgEntitlementManagementAssignmentRequest -Full\n</code></pre>"},{"location":"blog/assign-entra-access-packages-powershell/#5-confirm-it-delivered","title":"5. Confirm it delivered","text":"<p>Assignment is asynchronous \u2014 the request goes <code>submitted \u2192 delivering \u2192 delivered</code>. Poll the request, not the assignment:</p> <pre><code>do {\n    Start-Sleep -Seconds 5\n    $state = (Get-MgEntitlementManagementAssignmentRequest -AccessPackageAssignmentRequestId $request.Id).State\n    Write-Host \"State: $state\"\n} while ($state -in @(\"submitted\", \"delivering\"))\n</code></pre>"},{"location":"blog/assign-entra-access-packages-powershell/#bulk-onboard-a-whole-cohort","title":"Bulk: onboard a whole cohort","text":"<p>The point of scripting this is doing it 50 times. Loop a list of UPNs, and don't let one failure abort the batch:</p> <pre><code>$upns = Get-Content .\\new-hires.txt\n\nforeach ($upn in $upns) {\n    try {\n        $u = Get-MgUser -UserId $upn -ErrorAction Stop\n        $body = @{\n            requestType = \"adminAdd\"\n            assignment  = @{\n                targetId           = $u.Id\n                assignmentPolicyId = $policy.Id\n                accessPackageId    = $pkg.Id\n            }\n        }\n        New-MgEntitlementManagementAssignmentRequest -BodyParameter $body | Out-Null\n        Write-Host \"[ok]   $upn\"\n    }\n    catch {\n        Write-Warning \"[fail] $upn \u2014 $($_.Exception.Message)\"\n    }\n}\n</code></pre> <p>Let the platform dedupe</p> <p>Re-running an <code>adminAdd</code> for someone who already has the package is harmless \u2014 Entitlement Management won't double-assign. That makes the loop safe to retry, which matters when item 37 of 50 fails on a transient error.</p> <p>That closes the loop on this PowerShell series: connect \u2192 audit \u2192 grant. From here it's the same pattern for any Graph resource \u2014 find the cmdlet, check the scope, script it.</p>"},{"location":"blog/audit-entra-users-powershell/","title":"Auditing Entra ID users with PowerShell","text":"<p>\"Who has access?\" is the question every security review opens with. With the Microsoft Graph PowerShell SDK you can answer it in a few lines: list every user, flag the guests and the dormant accounts, and hand security a CSV.</p> <p>This builds directly on connecting to Graph \u2014 make sure you can run <code>Get-MgContext</code> first.</p>"},{"location":"blog/audit-entra-users-powershell/#connect-with-read-scopes","title":"Connect with read scopes","text":"<p>Auditing is read-only. Request exactly that \u2014 plus <code>AuditLog.Read.All</code>, which is what unlocks the all-important last-sign-in data:</p> <pre><code>Connect-MgGraph -Scopes \"User.Read.All\", \"AuditLog.Read.All\"\n</code></pre>"},{"location":"blog/audit-entra-users-powershell/#list-users-and-only-the-properties-you-need","title":"List users \u2014 and only the properties you need","text":"<p><code>Get-MgUser</code> returns a slim default set. Ask explicitly for the fields you'll report on, or they come back empty:</p> <pre><code>$users = Get-MgUser -All -Property `\n  DisplayName, UserPrincipalName, AccountEnabled, UserType,\n  CreatedDateTime, SignInActivity\n\n$users |\n  Select-Object DisplayName, UserPrincipalName, UserType, AccountEnabled |\n  Format-Table -AutoSize\n</code></pre> <p><code>SignInActivity</code> needs the property AND the scope</p> <p><code>signInActivity</code> is only populated when you (1) request it via <code>-Property</code> and (2) connected with <code>AuditLog.Read.All</code>. Omit either and the field is silently null \u2014 which looks exactly like \"never signed in\". Don't confuse the two.</p>"},{"location":"blog/audit-entra-users-powershell/#find-the-guests","title":"Find the guests","text":"<p>External identities are the usual soft spot. Advanced queries (<code>-ConsistencyLevel eventual</code> plus a count) let you filter server-side:</p> <pre><code>$guests = Get-MgUser -All `\n  -Filter \"userType eq 'Guest'\" `\n  -ConsistencyLevel eventual `\n  -CountVariable guestCount `\n  -Property DisplayName, UserPrincipalName, CreatedDateTime\n\nWrite-Host \"$guestCount guest accounts found\"\n</code></pre>"},{"location":"blog/audit-entra-users-powershell/#flag-stale-accounts-90-days-dormant","title":"Flag stale accounts (90+ days dormant)","text":"<p>Combine \"enabled\" with \"hasn't signed in lately\". <code>SignInActivity</code> can be null for accounts that have genuinely never signed in \u2014 treat that as stale too:</p> <pre><code>$cutoff = (Get-Date).AddDays(-90)\n\n$stale = $users | Where-Object {\n    $_.AccountEnabled -and (\n        -not $_.SignInActivity.LastSignInDateTime -or\n        $_.SignInActivity.LastSignInDateTime -lt $cutoff\n    )\n}\n\nWrite-Host \"$($stale.Count) enabled accounts dormant for 90+ days\"\n</code></pre>"},{"location":"blog/audit-entra-users-powershell/#export-for-the-security-review","title":"Export for the security review","text":"<p>A timestamped CSV is what auditors actually want to receive:</p> <pre><code>$report = $users | Select-Object `\n  DisplayName,\n  UserPrincipalName,\n  UserType,\n  AccountEnabled,\n  CreatedDateTime,\n  @{ Name = \"LastSignIn\"; Expression = { $_.SignInActivity.LastSignInDateTime } }\n\n$path = \"EntraUserAudit_{0:yyyy-MM-dd}.csv\" -f (Get-Date)\n$report | Sort-Object LastSignIn | Export-Csv -Path $path -NoTypeInformation -Encoding UTF8\n\nWrite-Host \"Wrote $($report.Count) rows to $path\"\n</code></pre> <p>Calculated properties flatten nested objects</p> <p><code>signInActivity</code> is an object, so it won't land cleanly in a CSV column. The <code>@{ Name = ...; Expression = ... }</code> hashtable (a calculated property) pulls the nested value out into a flat column \u2014 the standard PowerShell idiom for this.</p> <p>Next in the series: going from read-only auditing to actually granting access \u2014 assigning Entra ID access packages with PowerShell.</p>"},{"location":"blog/connect-microsoft-graph-powershell/","title":"Connecting to Microsoft Graph with PowerShell","text":"<p>Almost every Entra ID automation I write starts the same way: connect to Microsoft Graph, request the least privilege I need, do the work, disconnect. Get this foundation right and everything else \u2014 users, groups, access packages \u2014 is just cmdlets on top.</p> <p>This post covers installing the SDK, the two auth models (delegated vs app-only), and the scope gotcha that trips everyone up the first time.</p>"},{"location":"blog/connect-microsoft-graph-powershell/#install-the-sdk","title":"Install the SDK","text":"<p>The Microsoft Graph PowerShell SDK ships as a meta-module (<code>Microsoft.Graph</code>) that pulls in every sub-module \u2014 handy but heavy. For day-to-day identity work I install only what I use:</p> <pre><code># Everything (large; ~40 sub-modules)\nInstall-Module Microsoft.Graph -Scope CurrentUser\n\n# Or just the bits this series uses (faster)\nInstall-Module Microsoft.Graph.Authentication -Scope CurrentUser\nInstall-Module Microsoft.Graph.Users -Scope CurrentUser\nInstall-Module Microsoft.Graph.Groups -Scope CurrentUser\nInstall-Module Microsoft.Graph.Identity.Governance -Scope CurrentUser\n</code></pre> <p>Scope CurrentUser avoids the admin prompt</p> <p><code>-Scope CurrentUser</code> installs into your profile, so you don't need an elevated shell. On a locked-down work machine that's often the only option.</p>"},{"location":"blog/connect-microsoft-graph-powershell/#delegated-sign-in-you-interactively","title":"Delegated sign-in (you, interactively)","text":"<p>The quickest way in \u2014 a browser pops up, you sign in as yourself, and Graph acts as you with the scopes you request:</p> <pre><code>Connect-MgGraph -Scopes \"User.Read.All\", \"Group.Read.All\"\n</code></pre> <p>Check what you actually got \u2014 the token may have fewer scopes than you asked for if you lack consent:</p> <pre><code>Get-MgContext | Select-Object Account, TenantId, Scopes\n(Get-MgContext).Scopes\n</code></pre> <p>Scopes are additive across a session \u2014 but only after reconnect</p> <p>Calling <code>Connect-MgGraph</code> again with new scopes replaces the token; it doesn't merge. If a cmdlet fails with <code>Insufficient privileges</code>, reconnect with the full set you need:</p> <pre><code>Connect-MgGraph -Scopes \"User.Read.All\", \"Group.ReadWrite.All\", \"AuditLog.Read.All\"\n</code></pre>"},{"location":"blog/connect-microsoft-graph-powershell/#app-only-unattended-automation","title":"App-only (unattended automation)","text":"<p>For scheduled tasks and runbooks there's no human to click \"sign in\". You register an app in Entra, grant it application permissions (admin-consented), and authenticate with a certificate. Prefer a certificate over a client secret \u2014 secrets expire and leak; cert private keys can stay in the machine store.</p> <pre><code>Connect-MgGraph `\n  -ClientId  \"00000000-0000-0000-0000-000000000000\" `\n  -TenantId  \"contoso.onmicrosoft.com\" `\n  -CertificateThumbprint \"A1B2C3D4E5F6....\"\n</code></pre> <p>Delegated vs application scopes are different permissions</p> <p><code>User.Read.All</code> as a delegated scope lets the signed-in admin read users. As an application permission it lets the app read all users with no user present. They're granted separately in the app registration \u2014 don't assume consenting one covers the other.</p>"},{"location":"blog/connect-microsoft-graph-powershell/#find-the-cmdlet-and-the-permission-you-need","title":"Find the cmdlet (and the permission) you need","text":"<p>The SDK has thousands of cmdlets. Two helpers save a lot of portal-digging:</p> <pre><code># Which cmdlet maps to which Graph endpoint?\nFind-MgGraphCommand -Command Get-MgUser | Select-Object Command, Method, Uri\n\n# Which permissions does an endpoint require?\nFind-MgGraphPermission user -PermissionType Delegated\n</code></pre>"},{"location":"blog/connect-microsoft-graph-powershell/#always-disconnect-in-automation","title":"Always disconnect in automation","text":"<p>In a long-running session or a shared runbook host, leave no token behind:</p> <pre><code>Disconnect-MgGraph\n</code></pre> <p>That's the whole pattern: install \u2192 connect with least privilege \u2192 verify scopes \u2192 work \u2192 disconnect. The next post puts it to use auditing every user in the tenant.</p>"},{"location":"blog/entra-nextjs-getting-started/","title":"Skills Directory \u2014 Getting Started Guide","text":"<p>A complete, self-contained walkthrough for getting from zero to a working Entra-authenticated Next.js app on your laptop. Covers Day 0 (prerequisites), Day 1 (project on localhost), and Day 2 (Microsoft sign-in working end-to-end).</p> <p>This is the on-ramp to Sprint 1 of the project roadmap. By the end, you'll have a real, working foundation that everything else builds on.</p>"},{"location":"blog/entra-nextjs-getting-started/#day-0-prerequisites-23-hours","title":"Day 0 \u2014 Prerequisites (2\u20133 hours)","text":""},{"location":"blog/entra-nextjs-getting-started/#install-on-your-machine","title":"Install on your machine","text":"Tool Why How Node.js 22 LTS (or 25 Current) Runtime nvm recommended: <code>nvm install 22 &amp;&amp; nvm use 22</code>. Node 25 also works for dev \u2014 just set <code>\"engines\"</code> in package.json to target 22 for production parity with Vercel. pnpm Package manager (faster than npm, better monorepo support) <code>npm install -g pnpm</code> Git Version control Likely already installed VS Code (or Cursor) Editor With extensions: ESLint, Prettier, Prisma, Tailwind CSS IntelliSense GitHub CLI (<code>gh</code>) Repo creation, PR management <code>brew install gh</code> (Mac) or equivalent Docker Desktop (optional) Local Postgres if you don't want to use Neon for dev Skip if using Neon dev branches Azure CLI Entra app registration via CLI (faster than the portal) <code>brew install azure-cli</code> <p>Verify with: <pre><code>node --version    # v22.x or v25.x both fine\npnpm --version\ngit --version\ngh --version\naz --version\n</code></pre></p>"},{"location":"blog/entra-nextjs-getting-started/#create-accounts-all-free-tiers","title":"Create accounts (all free tiers)","text":"<ol> <li>GitHub \u2014 create a private repo named <code>skills-directory</code> (don't initialise it; you'll push from local).</li> <li>Vercel \u2014 sign up with your GitHub account. Free Hobby tier is fine for now.</li> <li>Neon \u2014 sign up with GitHub. Free tier gives you 1 project with branching. Create a project called <code>skills-directory</code> in EU Central region.</li> <li>Sentry \u2014 sign up, create a project of type \"Next.js\". Save the DSN.</li> <li>Microsoft Entra dev tenant \u2014 confirm you can sign in to <code>entra.microsoft.com</code>.</li> </ol> <p>That's the entire stack you need for Phase 1, Sprint 1. Nothing paid yet.</p>"},{"location":"blog/entra-nextjs-getting-started/#get-your-bearings-in-your-entra-dev-tenant","title":"Get your bearings in your Entra dev tenant","text":"<p>Before registering the app, log in to <code>entra.microsoft.com</code> and confirm:</p> <ul> <li>Your tenant ID \u2014 Overview \u2192 copy the Tenant ID (a UUID). You'll need this.</li> <li>You have admin rights \u2014 Roles &amp; admins \u2192 confirm Global Administrator or Application Administrator.</li> <li>You have at least 2\u20133 test users besides yourself \u2014 Users \u2192 if it's just you, create test users with mock job titles, departments, and photos. The directory needs something to render, and a \"directory of one\" is hard to demo.</li> </ul> <p>Tip: put fake but realistic data on those test users \u2014 different job titles, different departments. When you build the search/filter UI, real-looking data makes the UX easier to evaluate.</p>"},{"location":"blog/entra-nextjs-getting-started/#day-1-get-the-app-running-on-localhost-46-hours","title":"Day 1 \u2014 Get the app running on localhost (4\u20136 hours)","text":"<p>This is the \"I have something on <code>localhost:3000</code>\" milestone. No Entra yet, no DB queries \u2014 just the skeleton.</p>"},{"location":"blog/entra-nextjs-getting-started/#step-1-initialise-the-repo","title":"Step 1: Initialise the repo","text":"<pre><code>mkdir skills-directory &amp;&amp; cd skills-directory\ngit init\ngh repo create skills-directory --private --source=. --remote=origin\n</code></pre>"},{"location":"blog/entra-nextjs-getting-started/#step-2-set-up-the-monorepo","title":"Step 2: Set up the monorepo","text":"<pre><code>pnpm init\n# Edit package.json \u2014 add \"private\": true and the workspace config below\n</code></pre> <p>Create <code>pnpm-workspace.yaml</code>: <pre><code>packages:\n  - \"apps/*\"\n  - \"packages/*\"\n</code></pre></p> <p>Create the directory structure: <pre><code>mkdir -p apps/web packages/shared packages/config infra docs/adr docs/compliance docs/runbooks\n</code></pre></p>"},{"location":"blog/entra-nextjs-getting-started/#step-3-scaffold-the-nextjs-app","title":"Step 3: Scaffold the Next.js app","text":"<pre><code>cd apps/web\npnpm create next-app@latest . --typescript --tailwind --app --src-dir --import-alias \"@/*\" --eslint --no-turbopack\n</code></pre> <p>When prompted, accept defaults. This gives you Next.js 15 with App Router, TypeScript strict, Tailwind, and ESLint pre-configured.</p>"},{"location":"blog/entra-nextjs-getting-started/#step-4-pin-node-version-for-production-parity","title":"Step 4: Pin Node version for production parity","text":"<p>Edit <code>apps/web/package.json</code> and add: <pre><code>{\n  \"engines\": {\n    \"node\": \"&gt;=22 &lt;23\"\n  }\n}\n</code></pre></p> <p>This tells Vercel to use Node 22 LTS in production, regardless of what version you develop on locally.</p>"},{"location":"blog/entra-nextjs-getting-started/#step-5-add-shadcnui","title":"Step 5: Add shadcn/ui","text":"<pre><code># Still in apps/web/\npnpm dlx shadcn@latest init\n</code></pre> <p>Choose: New York style, Slate base color, CSS variables yes. Then add base components:</p> <pre><code>pnpm dlx shadcn@latest add button card input label avatar badge dialog dropdown-menu sonner\n</code></pre>"},{"location":"blog/entra-nextjs-getting-started/#step-6-install-core-dependencies","title":"Step 6: Install core dependencies","text":"<pre><code>pnpm add @azure/msal-node @azure/msal-react @azure/msal-browser @microsoft/microsoft-graph-client\npnpm add @prisma/client zod react-hook-form @hookform/resolvers\npnpm add @tanstack/react-query iron-session jose\npnpm add @sentry/nextjs pino\npnpm add -D prisma vitest @playwright/test @types/node\n</code></pre>"},{"location":"blog/entra-nextjs-getting-started/#step-7-set-up-prisma-with-two-schemas","title":"Step 7: Set up Prisma with two schemas","text":"<p>The control plane and tenant data plane each need their own Prisma schema.</p> <pre><code>pnpm prisma init\nmkdir -p prisma/control prisma/tenant\nmv prisma/schema.prisma prisma/control/schema.prisma\n</code></pre> <p>Create <code>apps/web/prisma/control/schema.prisma</code>: <pre><code>generator client {\n  provider = \"prisma-client-js\"\n  output   = \"../../node_modules/.prisma/control\"\n}\n\ndatasource db {\n  provider = \"postgresql\"\n  url      = env(\"CONTROL_DATABASE_URL\")\n}\n\nmodel Tenant {\n  id              String   @id @default(uuid())\n  displayName     String\n  entraTenantId   String   @unique\n  regionCode      String   @default(\"eu-west-1\")\n  status          String   @default(\"active\") // active | suspended | offboarding\n  dpaSignedAt     DateTime?\n  createdAt       DateTime @default(now())\n  updatedAt       DateTime @updatedAt\n\n  @@map(\"tenants\")\n}\n\nmodel Region {\n  code                  String   @id // e.g. \"eu-west-1\", \"za-north-1\"\n  displayName           String\n  connectionStringRef   String   // name of the Vercel env var, not the value\n  isDefault             Boolean  @default(false)\n  createdAt             DateTime @default(now())\n\n  @@map(\"regions\")\n}\n</code></pre></p> <p>Create <code>apps/web/prisma/tenant/schema.prisma</code>: <pre><code>generator client {\n  provider = \"prisma-client-js\"\n  output   = \"../../node_modules/.prisma/tenant\"\n}\n\ndatasource db {\n  provider = \"postgresql\"\n  url      = env(\"TENANT_DATABASE_URL\")\n}\n\nmodel User {\n  id              String   @id @default(uuid())\n  tenantId        String\n  externalId      String   // Entra OID\n  provider        String   @default(\"entra\")\n  email           String\n  displayName     String\n  jobTitle        String?\n  department      String?\n  officeLocation  String?\n  photoUrl        String?\n  photoHash       String?\n  disabledAt      DateTime?\n  createdAt       DateTime @default(now())\n  updatedAt       DateTime @updatedAt\n\n  userSkills      UserSkill[]\n  certifications  Certification[]\n\n  @@unique([tenantId, externalId, provider])\n  @@index([tenantId])\n  @@map(\"users\")\n}\n\nmodel Skill {\n  id           String   @id @default(uuid())\n  tenantId     String\n  name         String\n  category     String?\n  isCurated    Boolean  @default(false)\n  createdAt    DateTime @default(now())\n\n  userSkills   UserSkill[]\n\n  @@unique([tenantId, name])\n  @@index([tenantId])\n  @@map(\"skills\")\n}\n\nmodel UserSkill {\n  id           String   @id @default(uuid())\n  tenantId     String\n  userId       String\n  skillId      String\n  proficiency  String   // \"Beginner\" | \"Intermediate\" | \"Advanced\" | \"Expert\"\n  createdAt    DateTime @default(now())\n\n  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)\n  skill        Skill    @relation(fields: [skillId], references: [id], onDelete: Cascade)\n\n  @@unique([tenantId, userId, skillId])\n  @@index([tenantId])\n  @@map(\"user_skills\")\n}\n\nmodel Certification {\n  id            String   @id @default(uuid())\n  tenantId      String\n  userId        String\n  name          String\n  issuer        String\n  issuedAt      DateTime?\n  expiresAt     DateTime?\n  credentialUrl String?\n  createdAt     DateTime @default(now())\n\n  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)\n\n  @@index([tenantId])\n  @@map(\"certifications\")\n}\n\nmodel AuditLog {\n  id              String   @id @default(uuid())\n  tenantId        String\n  actorUserId     String?\n  actorExternalId String?\n  action          String   // e.g. \"user.signin\", \"skill.add\", \"profile.view\"\n  resourceType    String?\n  resourceId      String?\n  metadata        Json?\n  ipAddress       String?\n  userAgent       String?\n  createdAt       DateTime @default(now())\n\n  @@index([tenantId, createdAt])\n  @@index([tenantId, actorUserId])\n  @@map(\"audit_log\")\n}\n</code></pre></p>"},{"location":"blog/entra-nextjs-getting-started/#step-8-wire-up-environment-variables","title":"Step 8: Wire up environment variables","text":"<p>Create <code>apps/web/.env.local</code> (gitignored):</p> <pre><code># Control plane DB (your Neon project)\nCONTROL_DATABASE_URL=\"postgresql://...@ep-xxx.eu-central-1.aws.neon.tech/skills_control?sslmode=require\"\n\n# Default tenant region DB\nTENANT_DATABASE_URL_EU_WEST_1=\"postgresql://...@ep-xxx.eu-central-1.aws.neon.tech/skills_tenant_eu?sslmode=require\"\n\n# Entra (you'll fill these in on Day 2)\nENTRA_CLIENT_ID=\"\"\nENTRA_CLIENT_SECRET=\"\"\nENTRA_TENANT_ID=\"common\"\nENTRA_REDIRECT_URI=\"http://localhost:3000/api/auth/callback\"\n\n# Session\nSESSION_SECRET=\"generate_a_64_char_random_string_here\"\n\n# Sentry\nNEXT_PUBLIC_SENTRY_DSN=\"\"\nSENTRY_AUTH_TOKEN=\"\"\n</code></pre> <p>Generate the session secret: <pre><code>node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\"\n</code></pre></p> <p>Also create <code>apps/web/.env.example</code> with the same keys but no values \u2014 commit this so future-you (or a collaborator) knows what's needed.</p>"},{"location":"blog/entra-nextjs-getting-started/#step-9-create-your-two-databases-on-neon","title":"Step 9: Create your two databases on Neon","text":"<p>In the Neon console: 1. Your existing project (let's call it <code>skills-directory</code>) already has a default database. Rename it <code>skills_control</code>, or create a new one with that name. 2. Add a second database in the same project: <code>skills_tenant_eu</code>. 3. Copy both connection strings into <code>.env.local</code>.</p>"},{"location":"blog/entra-nextjs-getting-started/#step-10-run-your-first-migrations","title":"Step 10: Run your first migrations","text":"<pre><code># From apps/web/\npnpm prisma migrate dev --schema=./prisma/control/schema.prisma --name init\npnpm prisma migrate dev --schema=./prisma/tenant/schema.prisma --name init\n</code></pre> <p>If both succeed, you have two databases with empty tables. This is a milestone \u2014 celebrate it.</p>"},{"location":"blog/entra-nextjs-getting-started/#step-11-run-the-dev-server","title":"Step 11: Run the dev server","text":"<pre><code>pnpm dev\n</code></pre> <p>Open <code>http://localhost:3000</code>. You'll see the default Next.js landing page.</p> <p>Day 1 done. You have a running Next.js app, two databases, and your project structure. No auth yet, but the foundations are real.</p>"},{"location":"blog/entra-nextjs-getting-started/#day-2-get-entra-sign-in-working-46-hours","title":"Day 2 \u2014 Get Entra sign-in working (4\u20136 hours)","text":"<p>This is the moment of truth \u2014 when \"I built a Next.js app\" becomes \"I built a Next.js app that signs in with Microsoft.\"</p>"},{"location":"blog/entra-nextjs-getting-started/#step-1-register-your-app-in-entra","title":"Step 1: Register your app in Entra","text":"<p>The CLI way (faster):</p> <pre><code>az login --tenant &lt;YOUR_DEV_TENANT_ID&gt;\n\naz ad app create \\\n  --display-name \"Skills Directory (dev)\" \\\n  --sign-in-audience AzureADMultipleOrgs \\\n  --web-redirect-uris \"http://localhost:3000/api/auth/callback\" \\\n  --enable-id-token-issuance true \\\n  --required-resource-accesses '[\n    {\n      \"resourceAppId\": \"00000003-0000-0000-c000-000000000000\",\n      \"resourceAccess\": [\n        {\"id\": \"e1fe6dd8-ba31-4d61-89e7-88639da4683d\", \"type\": \"Scope\"},\n        {\"id\": \"b340eb25-3456-403f-be2f-af7a0d370277\", \"type\": \"Scope\"}\n      ]\n    }\n  ]'\n</code></pre> <p>The two scopes are <code>User.Read</code> and <code>User.ReadBasic.All</code> (delegated).</p> <p>This returns an <code>appId</code> \u2014 that's your <code>ENTRA_CLIENT_ID</code>. Save it.</p> <p>Create a client secret: <pre><code>az ad app credential reset --id &lt;YOUR_APP_ID&gt; --display-name \"dev-secret\" --years 1\n</code></pre></p> <p>This returns a <code>password</code> \u2014 that's your <code>ENTRA_CLIENT_SECRET</code>. You only see this once \u2014 copy it immediately into <code>.env.local</code>.</p> <p>Portal alternative: <code>entra.microsoft.com \u2192 App registrations \u2192 New registration</code>. Name it \"Skills Directory (dev)\", choose \"Accounts in any organisational directory\", redirect URI <code>http://localhost:3000/api/auth/callback</code>, register. Then API permissions \u2192 Add \u2192 Microsoft Graph \u2192 Delegated \u2192 <code>User.Read</code>, <code>User.ReadBasic.All</code>. Then Certificates &amp; secrets \u2192 New client secret.</p>"},{"location":"blog/entra-nextjs-getting-started/#step-2-build-the-auth-abstraction-the-strategy-interface","title":"Step 2: Build the auth abstraction (the strategy interface)","text":"<p>Create <code>apps/web/src/lib/auth/identity-provider.ts</code>:</p> <pre><code>export interface NormalizedIdentity {\n  externalId: string;\n  provider: 'entra' | 'workos' | 'google' | 'okta';\n  entraTenantId: string;\n  email: string;\n  emailVerified: boolean;\n  displayName: string;\n  rawClaims: Record&lt;string, unknown&gt;;\n}\n\nexport interface IdentityProvider {\n  getAuthorizationUrl(state: string, nonce: string): Promise&lt;string&gt;;\n  handleCallback(code: string, expectedNonce: string): Promise&lt;NormalizedIdentity&gt;;\n  signOut(postLogoutRedirectUri: string): Promise&lt;string&gt;;\n}\n</code></pre> <p>Create <code>apps/web/src/lib/auth/providers/msal-provider.ts</code>:</p> <pre><code>import { ConfidentialClientApplication, type Configuration } from '@azure/msal-node';\nimport { jwtVerify, createRemoteJWKSet } from 'jose';\nimport type { IdentityProvider, NormalizedIdentity } from '../identity-provider';\n\nconst config: Configuration = {\n  auth: {\n    clientId: process.env.ENTRA_CLIENT_ID!,\n    clientSecret: process.env.ENTRA_CLIENT_SECRET!,\n    authority: `https://login.microsoftonline.com/${process.env.ENTRA_TENANT_ID ?? 'common'}`,\n  },\n};\n\nconst msalClient = new ConfidentialClientApplication(config);\n\nconst SCOPES = ['openid', 'profile', 'email', 'User.Read', 'User.ReadBasic.All'];\nconst REDIRECT_URI = process.env.ENTRA_REDIRECT_URI!;\n\nconst JWKS = createRemoteJWKSet(\n  new URL('https://login.microsoftonline.com/common/discovery/v2.0/keys'),\n);\n\nexport const msalProvider: IdentityProvider = {\n  async getAuthorizationUrl(state, nonce) {\n    return msalClient.getAuthCodeUrl({\n      scopes: SCOPES,\n      redirectUri: REDIRECT_URI,\n      state,\n      nonce,\n      responseMode: 'query',\n    });\n  },\n\n  async handleCallback(code, expectedNonce) {\n    const result = await msalClient.acquireTokenByCode({\n      code,\n      scopes: SCOPES,\n      redirectUri: REDIRECT_URI,\n    });\n\n    if (!result?.idToken) {\n      throw new Error('No ID token returned from Entra');\n    }\n\n    const { payload } = await jwtVerify(result.idToken, JWKS, {\n      audience: process.env.ENTRA_CLIENT_ID,\n    });\n\n    if (payload.nonce !== expectedNonce) {\n      throw new Error('Nonce mismatch');\n    }\n\n    return {\n      externalId: payload.oid as string,\n      provider: 'entra',\n      entraTenantId: payload.tid as string,\n      email: (payload.preferred_username ?? payload.email) as string,\n      emailVerified: true,\n      displayName: payload.name as string,\n      rawClaims: payload as Record&lt;string, unknown&gt;,\n    };\n  },\n\n  async signOut(postLogoutRedirectUri) {\n    return `https://login.microsoftonline.com/common/oauth2/v2.0/logout?post_logout_redirect_uri=${encodeURIComponent(postLogoutRedirectUri)}`;\n  },\n};\n</code></pre>"},{"location":"blog/entra-nextjs-getting-started/#step-3-build-the-session-helpers","title":"Step 3: Build the session helpers","text":"<p>Create <code>apps/web/src/lib/auth/session.ts</code>:</p> <pre><code>import { getIronSession, type IronSession, type SessionOptions } from 'iron-session';\nimport { cookies } from 'next/headers';\n\nexport interface AppSession {\n  userId?: string;\n  tenantId?: string;\n  externalId?: string;\n  email?: string;\n  displayName?: string;\n  authNonce?: string; // used during auth flow only\n  authState?: string; // used during auth flow only\n}\n\nconst sessionOptions: SessionOptions = {\n  password: process.env.SESSION_SECRET!,\n  cookieName: 'skills_directory_session',\n  cookieOptions: {\n    secure: process.env.NODE_ENV === 'production',\n    httpOnly: true,\n    sameSite: 'lax',\n    maxAge: 60 * 60 * 8, // 8 hours\n  },\n};\n\nexport async function getSession(): Promise&lt;IronSession&lt;AppSession&gt;&gt; {\n  return getIronSession&lt;AppSession&gt;(await cookies(), sessionOptions);\n}\n</code></pre>"},{"location":"blog/entra-nextjs-getting-started/#step-4-sign-in-route","title":"Step 4: Sign-in route","text":"<p>Create <code>apps/web/src/app/api/auth/signin/route.ts</code>:</p> <pre><code>import { NextResponse } from 'next/server';\nimport { randomBytes } from 'node:crypto';\nimport { msalProvider } from '@/lib/auth/providers/msal-provider';\nimport { getSession } from '@/lib/auth/session';\n\nexport async function GET() {\n  const state = randomBytes(16).toString('hex');\n  const nonce = randomBytes(16).toString('hex');\n\n  const session = await getSession();\n  session.authState = state;\n  session.authNonce = nonce;\n  await session.save();\n\n  const url = await msalProvider.getAuthorizationUrl(state, nonce);\n  return NextResponse.redirect(url);\n}\n</code></pre>"},{"location":"blog/entra-nextjs-getting-started/#step-5-callback-route","title":"Step 5: Callback route","text":"<p>Create <code>apps/web/src/app/api/auth/callback/route.ts</code>:</p> <pre><code>import { NextResponse, type NextRequest } from 'next/server';\nimport { msalProvider } from '@/lib/auth/providers/msal-provider';\nimport { getSession } from '@/lib/auth/session';\n\nexport async function GET(req: NextRequest) {\n  const url = new URL(req.url);\n  const code = url.searchParams.get('code');\n  const state = url.searchParams.get('state');\n\n  if (!code || !state) {\n    return NextResponse.redirect(new URL('/?error=missing_code', req.url));\n  }\n\n  const session = await getSession();\n\n  if (state !== session.authState || !session.authNonce) {\n    return NextResponse.redirect(new URL('/?error=invalid_state', req.url));\n  }\n\n  try {\n    const identity = await msalProvider.handleCallback(code, session.authNonce);\n\n    // For Day 2, we just store the identity \u2014 no DB upsert yet.\n    // Sprint 2 adds tenant resolution + user upsert.\n    session.externalId = identity.externalId;\n    session.email = identity.email;\n    session.displayName = identity.displayName;\n    session.authState = undefined;\n    session.authNonce = undefined;\n    await session.save();\n\n    return NextResponse.redirect(new URL('/me', req.url));\n  } catch (err) {\n    console.error('Auth callback failed', err);\n    return NextResponse.redirect(new URL('/?error=auth_failed', req.url));\n  }\n}\n</code></pre>"},{"location":"blog/entra-nextjs-getting-started/#step-6-a-youre-signed-in-page","title":"Step 6: A \"you're signed in\" page","text":"<p>Create <code>apps/web/src/app/me/page.tsx</code>:</p> <pre><code>import { redirect } from 'next/navigation';\nimport { getSession } from '@/lib/auth/session';\n\nexport default async function MePage() {\n  const session = await getSession();\n\n  if (!session.externalId) {\n    redirect('/');\n  }\n\n  return (\n    &lt;div className=\"mx-auto max-w-2xl p-8\"&gt;\n      &lt;h1 className=\"text-2xl font-semibold\"&gt;Signed in&lt;/h1&gt;\n      &lt;dl className=\"mt-6 grid grid-cols-2 gap-4 text-sm\"&gt;\n        &lt;dt className=\"font-medium\"&gt;Display name&lt;/dt&gt;\n        &lt;dd&gt;{session.displayName}&lt;/dd&gt;\n        &lt;dt className=\"font-medium\"&gt;Email&lt;/dt&gt;\n        &lt;dd&gt;{session.email}&lt;/dd&gt;\n        &lt;dt className=\"font-medium\"&gt;External ID&lt;/dt&gt;\n        &lt;dd className=\"font-mono text-xs\"&gt;{session.externalId}&lt;/dd&gt;\n      &lt;/dl&gt;\n    &lt;/div&gt;\n  );\n}\n</code></pre>"},{"location":"blog/entra-nextjs-getting-started/#step-7-update-the-home-page-with-a-sign-in-button","title":"Step 7: Update the home page with a sign-in button","text":"<p>Replace <code>apps/web/src/app/page.tsx</code>:</p> <pre><code>import Link from 'next/link';\nimport { Button } from '@/components/ui/button';\n\nexport default function Home() {\n  return (\n    &lt;main className=\"flex min-h-screen flex-col items-center justify-center gap-6\"&gt;\n      &lt;h1 className=\"text-4xl font-bold\"&gt;Skills Directory&lt;/h1&gt;\n      &lt;p className=\"text-muted-foreground\"&gt;Sign in with your Microsoft account.&lt;/p&gt;\n      &lt;Button asChild&gt;\n        &lt;Link href=\"/api/auth/signin\"&gt;Sign in with Microsoft&lt;/Link&gt;\n      &lt;/Button&gt;\n    &lt;/main&gt;\n  );\n}\n</code></pre>"},{"location":"blog/entra-nextjs-getting-started/#step-8-the-moment-of-truth","title":"Step 8: The moment of truth","text":"<pre><code>pnpm dev\n</code></pre> <p>Open <code>http://localhost:3000</code>. Click sign in. You should be redirected to Microsoft, sign in with one of your dev tenant's test users, and bounce back to <code>/me</code> showing your name, email, and OID.</p> <p>If it works: you've crossed the hardest single mountain in this whole build. Entra OIDC working end-to-end is the gnarly bit. From here on, everything else is \"build features on top.\"</p>"},{"location":"blog/entra-nextjs-getting-started/#common-day-2-failure-modes","title":"Common Day 2 failure modes","text":"<ul> <li>\"AADSTS50011: redirect URI mismatch\" \u2014 your <code>.env.local</code> redirect URI doesn't exactly match what's registered in Entra. They must match character-for-character including the trailing slash (or lack thereof).</li> <li>\"invalid_state\" \u2014 your cookie isn't persisting. Check that you're on <code>http://localhost:3000</code> not <code>127.0.0.1:3000</code>.</li> <li>Nonce mismatch \u2014 usually a bug in how you're storing/reading the nonce in the session.</li> <li>Token validation fails \u2014 check your <code>ENTRA_TENANT_ID</code> is <code>common</code> if your app is multi-tenant, or your specific tenant ID if single-tenant.</li> <li>\"Cannot find module 'iron-session'\" or similar \u2014 make sure you're running commands from <code>apps/web/</code>, not the repo root.</li> </ul>"},{"location":"blog/entra-nextjs-getting-started/#what-you-have-at-end-of-day-2","title":"What you have at end of Day 2","text":"<p>A working multi-tenant-ready Next.js app with real Entra SSO, deployed to localhost. No directory features yet, but the auth foundation that everything else hangs off is solid and verified.</p>"},{"location":"blog/entra-nextjs-getting-started/#whats-next","title":"What's next","text":"<p>The remaining Sprint 1 tasks from the roadmap:</p> <ul> <li>Push to GitHub (you've already created the repo)</li> <li>Connect Vercel to the repo, set env vars in Vercel, deploy <code>staging.skillsdirectory.com</code></li> <li>Add Sentry DSN, verify error reporting</li> <li>GitHub Actions CI: lint + typecheck + test</li> </ul> <p>Sprint 2 starts the real product:</p> <ul> <li>Tenant resolution middleware (look up your Entra <code>tid</code> in the control plane, route to the right tenant DB)</li> <li>The Prisma extension that auto-injects <code>tenant_id</code> filters</li> <li>First Graph API call (<code>/me</code>) on sign-in to populate the <code>users</code> table</li> <li>Profile photo download and blob storage</li> </ul>"},{"location":"blog/entra-nextjs-getting-started/#a-founders-honest-warning","title":"A founder's honest warning","text":"<p>Days 1 and 2 will probably take longer than the time estimates above the first time you do this. Plan for a full weekend or four evenings. The friction points won't be the code \u2014 they'll be:</p> <ul> <li>Fighting Entra app registration \u2014 the portal UX is genuinely confusing, and the docs assume you know which permission ID maps to which scope.</li> <li>TLS/cookie issues on localhost \u2014 Iron-session in Lax-cookie mode is fine, but if you accidentally use a different domain you'll waste an hour.</li> <li>Prisma migration paths with two schemas \u2014 the dual-schema setup is unusual and you'll fight it once. After that it's fine.</li> </ul> <p>Don't be discouraged when one of these eats half a day. That's not a sign you're doing it wrong \u2014 that's the cost of building B2B SaaS infrastructure properly.</p> <p>When you hit a wall, paste the exact error and the file you're working in and someone (or your future self looking at this doc) can debug. Don't go round in circles for more than 30 minutes \u2014 ask.</p>"},{"location":"blog/entra-nextjs-getting-started/#notes-on-node-version","title":"Notes on Node version","text":"<p>You're running Node v25.2.1. That's fine for development. The <code>\"engines\": { \"node\": \"&gt;=22 &lt;23\" }</code> field in <code>apps/web/package.json</code> tells Vercel to use Node 22 LTS in production. This means:</p> <ul> <li>Local dev: Node 25 (your current version)</li> <li>Vercel production: Node 22 LTS</li> <li>CI (GitHub Actions): match Vercel \u2014 set <code>node-version: '22'</code> in workflow files</li> </ul> <p>If you ever see \"works locally, breaks in Vercel\" \u2014 Node version drift is the first thing to check.</p>"},{"location":"blog/the-great-unbundling/","title":"The Great Unbundling","text":""},{"location":"blog/the-great-unbundling/#why-ai-agents-are-quietly-dismantling-the-saas-economy-and-forcing-the-mag-7-to-pivot-before-the-window-closes","title":"Why AI agents are quietly dismantling the SaaS economy \u2014 and forcing the Mag 7 to pivot before the window closes","text":"<p>There's a quiet assumption baked into today's tech narrative: that the Mag 7 have already won. That their scale, distribution, and balance sheets make them untouchable in the age of AI.</p> <p>I think the opposite is true.</p> <p>They have time. What they do with that time is what decides whether they stay dominant or become the next generation of legacy infrastructure. Because the ground beneath them is shifting in a very specific way:</p> <p>AI isn't a feature. It's a new economic model.</p>"},{"location":"blog/the-great-unbundling/#saas-was-built-for-humans-ai-is-not","title":"SaaS was built for humans. AI is not.","text":"<p>The SaaS model is simple at its core: charge per user, per month, for software that amplifies human productivity.</p> <p>It made perfect sense in a world where work was done by people, software was the tool, and scale meant more seats.</p> <p>Agents break that equation.</p> <p>When two agents can do the work of fifty users, you don't need fifty licenses. You don't need ten. In the traditional sense, you might not need any. The pricing model collapses first. The product model follows.</p> <p>This is the real reason behind the slow, persistent compression in SaaS multiples \u2014 not that software is less valuable, but that the unit of value has changed.</p>"},{"location":"blog/the-great-unbundling/#from-seats-to-tokens","title":"From seats to tokens","text":"<p>What replaces per-user pricing? Tokenisation \u2014 not in the crypto sense, but in the economic one.</p> <p>You pay for compute. You pay for outcomes. You pay for execution, not access.</p> <p>Agents don't care about seats. They care about tokens processed, tasks completed, workflows executed. So the future pricing unit looks less like cost per employee and more like cost per task, cost per workflow, cost per decision.</p> <p>That's a problem if your entire revenue engine is built on expanding seat count.</p>"},{"location":"blog/the-great-unbundling/#the-agent-layer-is-the-new-interface","title":"The agent layer is the new interface","text":"<p>Here's the deeper disruption: SaaS tools are no longer the interface. Agents are.</p> <p>Instead of a human logging into five different systems, the agent orchestrates across them \u2014 pulling data, executing actions, returning outcomes.</p> <p>In that world, the UI becomes irrelevant. The product becomes an API. The brand becomes invisible.</p> <p>SaaS risks becoming commoditised infrastructure unless it owns \u2014 or embeds itself inside \u2014 the agent layer.</p>"},{"location":"blog/the-great-unbundling/#why-the-mag-7-still-have-a-window","title":"Why the Mag 7 still have a window","text":"<p>This is where the incumbents still have the edge. They control compute, distribution, developer ecosystems, and enterprise trust. More importantly, they have time and cash flow to spend.</p> <p>They can collapse pricing models before being forced to. They can bundle AI into existing ecosystems. They can acquire agent-native platforms. They can re-architect their products around workflows instead of features.</p> <p>The real question is whether they will. Incumbents rarely fail from lack of resources. They fail from protecting yesterday's margins.</p>"},{"location":"blog/the-great-unbundling/#the-strategic-fork","title":"The strategic fork","text":"<p>Every major platform now faces the same choice.</p> <p>Option 1: Defend SaaS economics. Preserve per-seat pricing. Add AI as a premium feature. Optimise for short-term revenue.</p> <p>Option 2: Embrace the agent economy. Shift to usage- or outcome-based pricing. Build native agent orchestration. Accept short-term revenue disruption for long-term dominance.</p> <p>History is pretty clear about which path wins.</p>"},{"location":"blog/the-great-unbundling/#what-the-next-five-years-look-like","title":"What the next five years look like","text":"<p>Expect a sharp decline in per-seat pricing as the dominant model. Expect a wave of SaaS companies repositioning as \"AI platforms\" \u2014 some convincingly, most not. Expect agents to become the primary consumers of software, not humans. And expect a new layer of companies to emerge that sit above SaaS and orchestrate it.</p> <p>Most importantly, expect consolidation. Once software becomes interchangeable infrastructure, scale matters again \u2014 but in a very different way.</p>"},{"location":"blog/the-great-unbundling/#the-final-thought","title":"The final thought","text":"<p>The current narrative is that AI will augment SaaS. That's too optimistic.</p> <p>AI doesn't just enhance tools. It changes who \u2014 or what \u2014 uses them. And when the user changes from human to agent, everything from pricing to product design has to change with it.</p> <p>The Mag 7 aren't too big to fail.</p> <p>But they are big enough to pivot.</p> <p>The clock is ticking.</p>"},{"location":"blog/archive/2026/","title":"June 2026","text":""},{"location":"blog/category/powershell/","title":"PowerShell","text":""},{"location":"blog/category/identity/","title":"Identity","text":""},{"location":"blog/category/infrastructure/","title":"Infrastructure","text":""},{"location":"blog/category/ai/","title":"AI","text":""}]}